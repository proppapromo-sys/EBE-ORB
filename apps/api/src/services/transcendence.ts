/**
 * transcendence.ts — ORB's Transcendence Engine (#30): surpassing the assumptions of the system, not
 * improving within it. Resonance amplifies what works; Transcendence asks "why does this limitation
 * exist at all?" — humans can't fly, machines can't learn, information can't travel instantly: every
 * era's invisible assumptions were eventually transcended. The frame is first-principles: break the
 * problem down to fundamental truths, then ask which constraint is real and which is just inherited.
 *
 * Directive engine — it steers the council to find and challenge the limiting assumption and rebuild
 * from first principles, while staying grounded enough to name a real first step. Adds no store.
 */
export const TRANSCENDENCE_QUERY = /\b(transcend|break (?:through|past) (?:the|this|a) (?:limit|barrier|ceiling)|why (?:is this|does this|can'?t (?:we|i|it))|what'?s (?:actually )?(?:stopping|limiting|holding (?:me|us) back)|impossible|what if (?:we|i) (?:could|didn'?t have to)|first principles?|question the assumption|challenge the assumption|who said|the (?:real )?constraint|remove the (?:limit|barrier|constraint|ceiling)|break the rules?|reimagine what'?s possible|push (?:the|past the) (?:limit|boundary|frontier)|next frontier|10x (?:not|instead of) 10%)\b/i;

export const TRANSCENDENCE_DIRECTIVE = ' Challenge the limit, don\'t just work within it: name the assumption everyone treats as fixed and ask why it exists at all. Break the problem to first principles — what is actually, physically, fundamentally true here — then separate the real constraints from the merely inherited ones, and rebuild from the truths that remain. Aim past incremental improvement toward what would be a step-change if the inherited limit fell. Stay honest and grounded: end on the smallest real experiment that would test whether the "impossible" thing is actually possible.';
